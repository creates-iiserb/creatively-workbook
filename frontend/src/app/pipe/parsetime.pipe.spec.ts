import { ParsetimePipe } from './parsetime.pipe';

describe('ParsetimePipe', () => {
  it('create an instance', () => {
    const pipe = new ParsetimePipe();
    expect(pipe).toBeTruthy();
  });
});
