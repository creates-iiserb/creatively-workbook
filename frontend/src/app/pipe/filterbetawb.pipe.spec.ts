import { FilterbetawbPipe } from './filterbetawb.pipe';

describe('FilterbetawbPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbetawbPipe();
    expect(pipe).toBeTruthy();
  });
});
