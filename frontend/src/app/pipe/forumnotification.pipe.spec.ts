import { ForumnotificationPipe } from './forumnotification.pipe';

describe('ForumnotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new ForumnotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
